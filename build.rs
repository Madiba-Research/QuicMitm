use std::io::Result;
fn main() -> Result<()> {
    // println!("cargo:warning=OUT_DIR is: {}", std::env::var("OUT_DIR").unwrap());
    // prost_build::compile_protos(&["src/httpreq.proto"], &["src/"])?;
    Ok(())
}